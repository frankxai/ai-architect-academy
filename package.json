{
  "name": "ai-architect-academy-site",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "screenshots": "node ./scripts/capture-screenshots.mjs",
    "optimize:svg": "svgo -f assets -o assets && svgo -f docs/assets -o docs/assets"
  },
  "devDependencies": {
    "puppeteer": "^22.12.1",
    "svgo": "^3.2.0"
  }
}
