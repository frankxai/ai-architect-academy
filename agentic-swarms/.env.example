# Copy to .env and set any provider keys you have. If none are set, the
# module runs in offline mock mode (deterministic responses for teaching).

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LITELLM_MODEL=gpt-4o-mini
TEMPERATURE=0.2
