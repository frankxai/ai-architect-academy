# Marketing Site (Prototype)

This folder contains a prototype marketing site. The canonical public media + publishing experience now lives in `publishing-engine/`.

If you are deploying the public site, use `publishing-engine/` as the Vercel root directory.
